{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python manage.py collectstatic --noinput && python manage.py migrate && python manage.py shell < create_admin.py"
  },
  "deploy": {
    "startCommand": "gunicorn online_exam_ai.wsgi --workers 4 --threads 4 --max-requests 1000",
    "restartPolicyType": "ON_FAILURE"
  }
}
